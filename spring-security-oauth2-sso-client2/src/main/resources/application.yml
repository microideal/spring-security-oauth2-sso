server:
  port: 8082
  servlet:
    session:
      cookie:
        name: SESSIONCOUPON

security:
  oauth2:
    client:
      client-id: CouponSystem
      client-secret: CouponSystem
      access-token-uri: http://localhost:8080/oauth/token
      user-authorization-uri: http://localhost:8080/oauth/authorize
    resource:
      user-info-uri: http://localhost:8080/user

spring:
  thymeleaf:
    cache: false